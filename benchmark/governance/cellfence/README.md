# CellFence Benchmark Mode

This mode runs the fixture's `cellfence.manifest.json` and optional `cellfence.baseline.json` through the built CLI after the replay is applied. It does not add benchmark-only hints to the task fixture.
